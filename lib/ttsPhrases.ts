// Fixed literal phrases spoken during a round, unrelated to the prompt
// bank. Shared between the pre-generation script and lib/tts.ts so both
// always agree on what's pregenerated.
export const FIXED_PHRASES = ["30 seconds left", "10 seconds left"];
